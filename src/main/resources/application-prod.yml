spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database: POSTGRESQL
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://localhost:5432/almacen_servipac_db
    username: postgres
    password: 123456
    driver-class-name: org.postgresql.Driver
server:
  port: 8080